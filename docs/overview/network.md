# Network Viz Graph

COBLE can output a network graph showing the dependencies and where possible additional informaton such as date.

This can run seperately with the command `network`:
```bash
coble network --frozen my-env_feeze.cbl --env my-env
```
**Note this is beta-test functionality**

![alt text](../imgs/network.png)