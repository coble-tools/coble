# Anatomy of a coble recipe file

This page describes the required and optional sections and layours of a coble `.cbl` file.

The format looks like yaml, but due to some of the file being sequential and optionally repeatable, is not valid yaml. The file uses yaml like headers to indicate sections and directives.

## Required beginning sections

```yaml

```

